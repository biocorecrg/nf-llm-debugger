package nextflow.debugger

import nextflow.plugin.BasePlugin
import org.pf4j.PluginWrapper

class DebuggerPlugin extends BasePlugin {
    DebuggerPlugin(PluginWrapper wrapper) {
        super(wrapper)
    }
}
